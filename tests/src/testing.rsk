; COMPILER MIGRATION VALIDATION

F addw(a, b) {
    r := a + b
}

F addf(a: f, b: f) {
    r := a + b
}

F main() {
    x := 5
    y := 10
    z := addw(x, y)
    printw(z)

    fx := 2.5f
    fy := 1.5f
    fz := addf(fx, fy)
    printf(fz)

    i := 0


    r := 0
}