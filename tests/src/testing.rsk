; COMPILER MIGRATION VALIDATION

F addw(a, b) {
r := a + b
}

F addf(a: f, b: f) {
r := a + b
}

F main() {
; --- TEST 3: LF, manual increment (auto-increment not implemented yet) ---
k := 1
LF (k := 1 until 10) {
    printw(k)
}

r := 0
}